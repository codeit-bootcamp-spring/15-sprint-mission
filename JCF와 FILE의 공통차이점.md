
# 공통점
* UUID로 객체를 구분한다.
* File*Service 구현체로 저장하는 객체-UUID.ser의 모습이 JCF*Service 구현체의 JCF처럼 하나의 이름으로 데이터가 연속해서 저장되는 것이 서로 닮았다고 생각이 든다.

# 차이점
* JCF*Service를 사용하면 데이터 저장소로 JCF를 사용하기 때문에 사용은 간편하지만 각 실행마다 이전 데이터가 사라지는 반면에 File*Service를 사용하면 파일 입출력 라이브러리를 사용하기 때문에 신경써야 할 것이 많지만 실행이 종료되더라도 데이터가 살아남아있다.
* File*Service를 사용하면 재 실행으로 저장되어 있던 데이터들을 전부 불러오거나, 저장하는 것처럼 UUID로 객체를 찾는 것이 아닌 경우에는 문제없이 사용이 가능하지만 객체의 UUID를 사용하여 기존 객체를 찾는 '단일 조회'(`read()`)나, '업데이트'(`update()`)의 경우 사용이 불가능하다.
> File*Service를 사용하면 단일 조회와 업데이트가 왜 불가능한가?
> 
> * 각 실행 마다 동일한 멤버변수를 가지는 객체를 생성하더라도, 부여받는 UUID가 달라진다.
> * 예를 들어, 첫 실행시 User의 UUID가 'c19'로 시작하는 UUID를 가졌다고 치자. 이 User를 다음 실행시 동일한 매개변수로 생성자를 통해 생성하더라도 UUID는 'c19'로 시작하지 않고 달라진다.
> * 그러므로, 지금처럼 UUID를 객체 식별의 기준점으로 잡고 서비스를 하는 것은 불충분하다.
> 
> 해결 방법?
> 
>  * 가장 간단하게 드는 생각은 객체를 식별하는데 다른 매개변수를 받는 것이다. User의 경우는 각 User의 name을 매개변수로 받거나, Channel의 경우는 Channel name을 받으면 되겠지만, 이는 우리가 처음에 UUID를 객체 식별자로 선언했을 때와는 다르게 UUID의 의미가 심하게 퇴색되며 각 Entity마다 통합되지 않아 추후에 문제가 생길 것 같다.
>  * 그 외에는 아직까지는 잘 모르겠다.

# 비즈니스 로직
* Entity 저장시 중복인지 아닌지
* Entity 업데이트시 중복인지 아닌지와 찾을 수 없는 경우
* Entity 삭제시 찾을 수 없는 경우
* Entity 구조가 올바른지 정책 설정

# 저장 로직
* 저장 로직은 비즈니스 로직이 넘겨주는 객체를 무조건 CRUD 해보고 안되면 비즈니스 로직에게 안된다고 보고하는 것 뿐만 하면 된다.
* 파일 입출력 라이브러리
* 파일의 CRUD
